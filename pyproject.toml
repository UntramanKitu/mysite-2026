[project]
name = "mysite-2026"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django",
    "pillow>=12.3.0",
]

[dependency-groups]
dev = [
    "ipython>=9.15.0",
]
